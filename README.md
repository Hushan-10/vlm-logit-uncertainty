# vlm-logit-uncertainty
Extract logit-based uncertainty from Vision-Language Models (VLMs) using per-token log probabilities, entropy, and top-k token “flicker” to estimate confidence and detect hallucinations
